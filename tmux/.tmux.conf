set-window-option -g mode-keys vi
bind-key -t vi-copy 'v' begin-selection
bind-key -t vi-copy 'y' copy-selection
bind -n C-k clear-history
bind p paste
bind Escape copy-mode
unbind [
unbind ]
bind y run-shell "tmux show-buffer | xclip -sel clip -i"
set -s escape-time 0
set -g default-terminal "screen-256color"
set-option -g history-limit 3000
unbind C-b
set -g prefix ^A
bind a send-prefix
# ## set status bar
# set -g status-bg "#373b41"
# set -g status-fg "#c5c8c6"

# ## highlight active window
# setw -g window-status-current-bg "#282a2e"
# setw -g window-status-current-fg "#81a2be"

# ## highlight activity in status bar
# setw -g window-status-activity-fg "#8abeb7"
# setw -g window-status-activity-bg "#1d1f21"

# ## pane border and colors
# set -g pane-active-border-bg default
# set -g pane-active-border-fg "#373b41"
# set -g pane-border-bg default
# set -g pane-border-fg "#373b41"

# set -g clock-mode-colour "#81a2be"
# set -g clock-mode-style 24

# set -g message-bg "#8abeb7"
# set -g message-fg "#000000"

# set -g message-command-bg "#8abeb7"
# set -g message-command-fg "#000000"

# set -g mode-bg "#8abeb7"
# set -g mode-fg "#000000"## set status bar
# set -g status-bg "#373b41"
# set -g status-fg "#c5c8c6"

# ## highlight active window
# setw -g window-status-current-bg "#282a2e"
# setw -g window-status-current-fg "#81a2be"

# ## highlight activity in status bar
# setw -g window-status-activity-fg "#8abeb7"
# setw -g window-status-activity-bg "#1d1f21"
# ## pane border and colors
# set -g pane-active-border-bg default
# set -g pane-active-border-fg "#373b41"
# set -g pane-border-bg default
# set -g pane-border-fg "#373b41"

# set -g clock-mode-colour "#81a2be"
# set -g clock-mode-style 24

# set -g message-bg "#8abeb7"
# set -g message-fg "#000000"

# set -g message-command-bg "#8abeb7"
# set -g message-command-fg "#000000"

# set -g mode-bg "#8abeb7"
# set -g mode-fg "#000000"

# run-shell "powerline-daemon -q"
# source "/home/maik/.local/lib/python3.5/site-packages/powerline/bindings/tmux/powerline.conf"
source-file "${HOME}/.config/tmux/solarized.theme"

### [](#install-using-tmux-plugin-manager)
